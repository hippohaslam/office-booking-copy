const postAssignedDesks = (desks: Array<Desk>) => {
  // TODO: Use axios or fetch to send a list of desks to the API.
  console.log('desks saved', desks);
}


const getDesks = (officeId: number) => {
  // TODO: Get all desks
  // Should be for specific office by an ID
  console.log(officeId)
}
